[tool.poetry]
name = "waypoint-converter"
version = "0.1.0"
description = "Text file reader and converter"
authors = ["Lukasz"]
package-mode = false

[tool.poetry.dependencies]
python = "^3.13"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
